import * as auth from "./authSelector";
import * as todo from "./todoSelector";
import * as todoFilter from "./todoFilterSelector";

export const authSelector = auth;
export const todoSelector = todo;
export const todoFilterSelector = todoFilter;
